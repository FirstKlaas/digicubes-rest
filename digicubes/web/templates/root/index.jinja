{% extends "account_base.jinja" %}

{% block main_menu %}
  <li><a href="{{ url_for('account.logout') }}">Logout</a></li>
{% endblock %}

{% block main_content %}
  <div class="row">
    <div class="col s3" id="dc-left-nav">
      <div class="divider"></div>
      <div class="section"><a href="{{ url_for('account.user_list') }}">Users<a></div>
      <div class="divider"></div>
      <div class="section"><a href="{{ url_for('account.user_list') }}">Roles</a></div>
      <div class="divider"></div>
      <div class="section"><a href="{{ url_for('account.right_test') }}">Test Rights</a></div>
      <div class="divider"></div>
      {% if current_user.has_right('klaas') %}
      <div class="section"><a href="{{ url_for('account.right_test') }}">Test Rights</a></div>
      <div class="divider"></div>
      {% endif %}
    </div>  
    <div class="col s9" id="dc-main"></div>
  </div>      
{% endblock %}        